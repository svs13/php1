Исполнитель: Владимир Сидоров.

п.1
    1. Раз передаётся имя файла гостевой, значит проверяем на существование этот файл.
    Если не существует - то создаём новый, пустой, с указанным именем файла.
    Данные, нужные объекту для работы:
    - путь файла гостевой
    - массив записей гостевой
    2. Сделано
    3. Сделано
    4. Сделано

п.2 Что вынести в класс TextFile из тех частей, что сделали.
данные файла должны быть защищены от прямого доступа ( по заданию )
    - хранение пути файла
    - хранение данных файла
    - получение данных файла
    - запись данных в файл
    Все операции с данными файла оставляем потомкам
    Т.к. файл текстовый, то данные храним в массиве построчно.
    не хватает метода write($data), но это уже не по дз.

п.3
    1. Имя поля формы храним в свойстве, т.е. потребуется.
    2. Добавлена ветка с is_uploaded_file, чтобы проверить действительно ли загружен файл через HTTP POST
    3. Не ясно где указывать постоянное место. Выяснил. upload($path)
Что будет когда файл переместили? isUploaded должен возвращать false и все остальные методы не должны действовать
добавил строку в isUploaded(): if ( file_exists( $_FILES[ $this->fn ]['tmp_name'] ) ) { // и если файл ещё не перемещён


