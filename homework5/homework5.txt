п.1
    1. Где хранить записи: логин и хэш пароля - так, чтобы он был не доступен клиенту?
    Приватные/закрытые "зоны" создавать не умею. Вариант на данный момент 1 - в теле функции.
    2. Как получить из двумерного массива [логин,пароль] только массив всех [логин]
    http://php.net/manual/ru/ref.array.php
    array_column — Возвращает массив из значений одного столбца входного массива
    3. Как получить из двумерного массива [логин,пароль] пароль по логину
    ищу подходящую ф-ю там же, надо заучивать и практикой закреплять чтобы знать имя ф-и кот-ю ищешь.
    нет готовой. Надо разбить на простые операции.
    Конечно, можно использовать foreach - но это уже если стандартными ф-ями не получится.

    Разбиваю операцию на более простые:
    - найти индекс по логину
        индекс по значению возвращает array_search(), но ей надо передать точное значени, которое нам известно, т.е.
        ['login' => $login ] и массив должен содержать только один логин (без изменения индексов).
        Получить такой массив как? array_column(). Но эта ф-я создаст новый массив и не факт что порядок ключей
        сохранится как в исходом. Всё становится слишком сложно для понимаю. Надо ПРОЩЕ и наглядней - а это foreach
    - по индексу получить пароль

    Использую foreach

п.2 Имя вошедшего на сайт пользователя где хранится? в куки сохранять их нельзя, не безопасно. Значит будем хранить
    на сервере в сессии (как по уроку).
    Значит получить имя пользователя сможем только по той сессии, которую он (клиент) указал в запросе.
    ф-и сессий http://php.net/manual/ru/ref.session.php

п.3
    1. Сделано.
    2. Формой отправляем данные методом post, чтобы  в адресе не показывало логин/хешпароля
    В форме входа в поле пароль не должны отображаться введенные символы. Использую <input type="password"
    3. Данные сохраняем в сессию (метим клиента). Сделано. Доп метить клиента не требуется.

п.4
    Загрузка изображения разрешена только авторизованным пользователям.
    Лог:
        кто? username
        когда? time() НЕТ. date();
        какое изобржение загружено? имя изображения





