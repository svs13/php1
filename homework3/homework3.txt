Исполнитель: Владимир Сидоров.

п1.
Для вставки введённых значений в input-ы ...
проверяем/записываем каждое введенное значение отдельно и если не было ошибок - высчитываем.
проверяем в обратном порядке, чтобы ошибки отображались последовательно.
Сделано.

п.2
Чтобы в 2-х разных программах (рhomework3.php и image.php) получить один и тот же массив картинок, используется отдельный
файл (funcrions.php) , которыя включается в первые две программы и позволяет получить массив данных картинок.

Массив картинок должен содержать id и url на картинку для каждой картинки:
[ id1=>url1, id2=>url2 ... ]

Для программы phomework3.php как вставить в html ... id и value каждого элемента массива картинок?
- 1 вариант: осуществить вывод в поток в процессе выполнения foreach
...
<a href="image.php?id=<?php echo $index; ?>"><img src="<?php echo $value; ?>" ></a>
...

- 2 вариант: создать новый многомерный массив [ 0=>[ id1, url1 ], 1=>[ id2, url2] ...]
и использовать этот массив при вставке значений. Зачем создавать новый? Индексы не известны - что вставлять в html?

Т.к. вёрстка обычно готова, логично использовать 2-й вариант.

Как преобразовать сущ. массив?
[ id1=>url1, id2=>url2 ... ] в [ 0=>[ id1, url1 ], 1=>[ id2, url2] ...]

почитав описание функций массивов http://php.net/manual/ru/ref.array.php

- 1 вар. использовать foreach
- 2 вар. использовать reset(), next(), key()...
- 3 вар. array_shift(), key()

Принимаю 1 вар., проще выводить в поток готовую переменную.

Сделано.
